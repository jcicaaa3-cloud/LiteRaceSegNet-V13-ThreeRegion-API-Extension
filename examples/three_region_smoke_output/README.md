# Synthetic Three-Region Smoke Output

This folder is generated by:

```bash
python scripts/test_three_region_postprocess_smoke.py
```

It is a synthetic code-smoke example, not V13 evidence and not a real road safety result.
It exists only to show the expected artifact names:

```text
v13_visualization.json
output/crack_mask.png
output/major_damage_mask.png
output/suspected_mask.png
output/three_region_overlay.png
```

For a real request, the API writes the same artifact names under:

```text
runtime_api/requests/<request_id>/
```
